# Identify local path to synced SharePoint site for each user.  This path
# is above the path for the Rproject and allows for reading of data higher
# in the directory tree.

userPath <- "SuRGE_Sharepoint/"